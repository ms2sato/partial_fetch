{
  "name": "partial_fetch",
  "version": "0.8.0",
  "description": "partial_fetch for ajax with refresh part of HTML(usualy called partial)",
  "main": "index.js",
  "scripts": {
    "clean": "npx rimraf dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ms2sato",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.2.4"
  }
}
